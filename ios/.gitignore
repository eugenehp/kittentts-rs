# ── macOS ─────────────────────────────────────────────────────────────────────
.DS_Store
**/.DS_Store

# ── Generated by build_rust_ios.sh ────────────────────────────────────────────
# Run `bash ios/build_rust_ios.sh` from the repo root to recreate.

# Compiled xcframework (Rust + ORT + espeak-ng statically merged)
KittenTTS.xcframework/

# Compiled espeak-ng phoneme tables (~300 files)
espeak-ng-data/

# Downloaded model files (~40 MB) — fetched from HuggingFace by the script
# or on first launch if absent from the bundle.
KittenTTSApp/KittenTTSApp/Models/

# ── Xcode ──────────────────────────────────────────────────────────────────────
# Per-user IDE state — changes on every build, meaningless to other developers.
KittenTTSApp/KittenTTSApp.xcodeproj/xcuserdata/
KittenTTSApp/KittenTTSApp.xcodeproj/project.xcworkspace/xcuserdata/

# SwiftPM resolution state (auto-regenerated by Xcode on open)
KittenTTSApp/KittenTTSApp.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/

# Binary workspace state snapshot (per-user, large, changes constantly)
*.xcuserstate
