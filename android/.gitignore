# ── macOS ─────────────────────────────────────────────────────────────────────
.DS_Store
**/.DS_Store

# ── Gradle ────────────────────────────────────────────────────────────────────
KittenTTSApp/.gradle/
KittenTTSApp/build/
KittenTTSApp/app/build/

# Machine-specific SDK/NDK paths — generated by Android Studio on first open.
KittenTTSApp/local.properties

# ── Generated by build_rust_android.sh ────────────────────────────────────────
# Run `bash android/build_rust_android.sh` from the repo root to recreate.

# Compiled shared libraries
KittenTTSApp/app/src/main/jniLibs/arm64-v8a/*.so
KittenTTSApp/app/src/main/jniLibs/x86_64/*.so

# Compiled espeak-ng phoneme data
KittenTTSApp/app/src/main/assets/espeak-ng-data.zip

# Downloaded model files (~40 MB) — fetched from HuggingFace by the script
# or on first launch if absent from assets.
KittenTTSApp/app/src/main/assets/models/
